# 生成输出

## 1. 目录职责

保存可以由源文档重新生成的渲染、导出和检查结果。

## 2. AI读取规则

视觉检查DOCX、PDF或其他导出结果时读取。

## 3. 文件规范

按输出任务建立子目录，保留来源和生成日期说明。

## 4. 禁止事项

不得把唯一原稿只保存在本目录，不得把输出误认为可编辑源文件。

## 5. 维护规则

源文档变化后重新生成；旧输出可移入`240_archive/`。
