# 接口与数据契约

## 1. 目录职责

记录教材Demo之间必须保持一致的ROS接口、参数和数据字段。

## 2. AI读取规则

修改Topic、Service、Action、参数名、节点名或记录格式前读取。

## 3. 文件规范

接口变化必须同步代码、README、契约和验证记录。

## 4. 禁止事项

不得只修改一章代码而破坏后续章节依赖。

## 5. 维护规则

每次接口变化后更新，并注明兼容影响。
